services:
  github-mcp-server:
    image: ghcr.io/github/github-mcp-server:latest
    env_file:
      - .env
    stdin_open: true
    tty: true
    volumes:
      - ./data/cache:/home/mcp/.cache
    restart: unless-stopped
